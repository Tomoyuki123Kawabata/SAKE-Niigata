<div class="container">
  <h1 class="mb-4">レビュー一覧</h1>
  <div class="row">
    <% @post_sakes.each do |post_sake| %>
      <div class="col-md-4 mb-4">
        <div class="card">
          <%= image_tag post_sake.image, class: "card-img-top", size: "200x200" %>
          <div class="card-body">
            <h5 class="card-title"><%= link_to post_sake.sake, post_sake_path(post_sake.id) %></h5>
            <p class="card-text"><%= post_sake.comment %></p>
            
          <% end %>
          </div>
        </div>
      </div>
